# 2026-08-20

- 本目录是 **SampleDir 官网（完整网站 + GitHub 展示仓库）的固定工作区**（用户 2026-08-20 明确"以后都在这里了"）。
- **官网全量文件在本目录**：index.html（中/英/韩/日四语、含 GitHub 导航、logo base64 内联）、assets/（css/js/img）、help.html、help_en.html、version.json、logo.svg、README.md（纯文字产品介绍 + logo.svg 真实图片引用）。
- git 仓库：remote=github.com/huoleihu/SampleDir，main 已设 upstream origin/main；.gitignore 忽略 .DS_Store/*.iml/.idea/.kotlin/*.log/.workbuddy/。
- **与主工程关联**：主工程 `/Users/huoleihu/ai_workbuddy/kotlin_sampleDir`（已删除 webPage/）负责开发与打包安装包；本目录负责对外展示与 GitHub Release 分发。
- 桌面安装包 SampleDir-1.0.3-arm64.{dmg,pkg} 在 ~/Desktop，待发 GitHub Release（本机无 gh CLI，走网页 Draft release，tag=v1.0.3）。
- **官网加「购买」入口（2026-08-20，未提交 git，用户自提）**：位置最终定为 ①右上角 CTA 区：「购买」按钮与「免费下载」按钮并排（nav.buy，btn--ghost nav__cta）；②Hero 区「下载 for macOS」旁「购买」ghost 按钮（hero.buy）。四语（zh 购买 / en Buy / ko 구매 / ja 購入）。**当前「暂不支持购买」置灰**：两处按钮均加 `btn--disabled`（CSS 新增 .btn--disabled：opacity .45 + pointer-events:none）+ `aria-disabled="true" tabindex="-1" title="暂不支持购买"`，href 保持 `#`。开放时：移除禁用属性并把 href 换成真实商店地址（Lemon Squeezy/Paddle/Gumroad）。曾试过放 nav__links 文字链接（下载后 / GitHub 后），均不理想，最终移出文字链接区改按钮。
- **一键发布脚本 `release.sh`（2026-08-20 新建，未提交 git）**：纯 curl 发布安装包到 GitHub Release，无需 gh CLI。用法 `./release.sh v1.0.3 ~/Desktop/*.dmg ~/Desktop/*.pkg`；无参运行时自动从文件名提版本号（`SampleDir-1.0.3-arm64.dmg` → `v1.0.3`）；tag 打在官网仓库（本目录）；token 读 `GITHUB_TOKEN` 环境变量或 `~/.sampledir_gh_token`（chmod 600）；幂等（release 已存在则复用继续传资产）。
- **v1.0.3 Release 已成功发布**（2026-08-20）：token 初始 403 失败（Fine-grained 默认无权限），网页加 **Contents: Read and write** 后成功。教训：Fine-grained token 与 Classic token 不同，权限默认零、必须显式勾选。后续若换 token 仍 403，先去网页检查 Permissions→Repositories→Contents 这一项是否就位（不必再生 token，Update 即可）。
- **端到端发布流程笔记**：完整步骤 + 排查 + 命名约定 + 兜底（网页手动）已写入 `~/.workbuddy/MEMORY.md`「## 发布流程」段；本目录重新打开也能看到，便于任何工作区执行。
- **网站埋点上线（档位 2，Pages Functions + CF Analytics Engine，2026-08-20，未提交 git）**：纯 CF 全家桶，零第三方、零 PII、零成本（Pages 10w 请求/天 + AE 几千万次写入/天免费）。
  - 新文件：`assets/js/track.js`（click 监听 + sendBeacon → '/api/track'，跳过 btn--disabled / aria-disabled，自动 PV）；`functions/api/track.js`（POST + OPTIONS，CORS 友好，writeDataPoint(blobs=[action,page,lang,ref], doubles=[ts], indexes=[action])）；`wrangler.toml`（dataset=sampledir_clicks，binding=ANALYTICS）。
  - index.html 加 8 处 `data-track`：gh-nav（GitHub 外链）、dl-cta-free（顶部「免费下载」）、dl-hero（Hero「下载 macOS」）、explore-hero（Hero「了解功能」）、dl-123（123云盘）、dl-quark（夸克）、contact-mail（mailto）、help-footer（帮助文档）。禁用按钮（购买）不计入。
  - 查询：`npx wrangler analytics --binding=ANALYTICS "SELECT blob1 AS action, count() AS hits FROM sampledir_clicks GROUP BY action ORDER BY hits DESC"`。
  - **必须提交到 git + 推到 GitHub 才能在部署里生效**（CF Pages 接 GitHub 自动 rebuild，wrangler.toml 自动创建 dataset）。用户说自己提交，所以本地只加文件，未做 git add/commit/push。
- **英文导航排版修复（2026-08-20，未提交 git）**：用户反馈英文下 `Get Started` / `Free Download` 被挤换行。根因：a 标签默认在空格断行 + nav__inner 默认 flex-wrap + body overflow-x:hidden 三者叠加导致。修：styles.css 加 `.nav__inner { flex-wrap: nowrap; min-width:0; overflow-x:auto; scrollbar-width:none; }` + 全部 nav 子元素加 `white-space:nowrap` + `flex-shrink:0` + 收紧 gap(26→22)、nav__cta padding(18→14) + lang margin(8→4)。语言 7 个字符（Get Started/Free Download）也能装下；窄屏横滚兜底，避免被 body 裁掉。
